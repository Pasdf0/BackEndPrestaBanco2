spring:
  application:
    name: simulate-service
  cloud:
    config:
      enabled: true
      uri: ${DB_URI}
  datasource:
    url: jdbc:postgresql://${DB_URL}/${DB_NAME_SIMULATE}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update